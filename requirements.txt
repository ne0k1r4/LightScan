# LightScan v2.0 PHANTOM — optional dependencies
# Core runs on pure stdlib. Install these for full capability.

# SSH brute
paramiko==3.4.0

# MySQL brute
pymysql==1.1.1

# PostgreSQL brute
psycopg2-binary==2.9.9

# SMB / NTLM / RDP / impacket-based attacks (secretsdump, psexec, etc.)
impacket==0.12.0

# LDAP brute + AD enumeration
ldap3==2.9.1

# VNC brute + crypto
pycryptodome==3.20.0

# MSSQL brute
pymssql==2.3.0

# Packet-level SYN scan (--syn, passive OS fingerprint)
scapy==2.5.0

# Async HTTP for web scanner / OAuth
aiohttp==3.9.5

# YAML template engine (--cve templates)
PyYAML==6.0.1
