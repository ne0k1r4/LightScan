id: CVE-2020-13401
name: CVE-2020-13401
severity: critical
tags: [docker, exposure, rce, container, cve-2020-13401]
port: 2375
protocol: http
description: Unauthenticated Docker API exposed
remediation: Bind to unix socket or enable TLS client auth
reference: https://docs.docker.com/engine/security/protect-access/
steps:
  - type: send
    data: /version
  - type: match
    contains: ApiVersion
    part: body
  - type: extract
    regex: '"Version":\s*"([^"]+)"'
    group: 1
    name: version
